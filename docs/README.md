# Raspberry Pi Remote Control System

본 프로젝트는 라즈베리 파이를 이용한 원격 하드웨어 제어 및 모니터링 시스템이다.


## 1) 프로젝트 구조


```bash
📁 rpi-remote-control-system
├── 📁 code
│   ├── 📁 client (클라이언트 소스 코드)
│   └── 📁 server (서버 및 장치 제어 소스 코드)
├── 📁 exec (실행 파일 및 동적 라이브러리 결과물)
└── Makefile (전체 프로젝트 빌드 스크립트)
```


## 2) 빌드 및 실행 방법


### 빌드 환경
- Board: Raspberry Pi 4 (B Model) 
- OS: Raspberry Pi OS (Linux)
- Library: wiringPi, pthread, dl

### 빌드 명령어

프로젝트 루트 디렉터리에서 `make` 명령을 실행한다.

```bash
$ make 
```

- `all` 타겟을 통해 `exec` 디렉터리에 서버/클라이언트 실행 파일이 생성되고, `exec/lib` 디렉터리에 장치별 동적 라이브러리(`.so`)가 생성된다.

### 실행 방법

1. **서버 실행 (터미널 1)** 
    
    ```bash
    $ cd exec
    $ sudo ./server
    ```
    
2. **클라이언트 실행 (터미널 2)**
    
    ```bash
    $ sudo ./client [서버_IP]
    ```

    

## 3) 평가 기준 항목 구현 여부

| **평가 항목** | **구현 여부** | **상세 내용** |
| --- | --- | --- |
| 하드웨어 제어 (LED) | O | SoftPWM을 이용한 0~100 단계 밝기 조절 및 ON/OFF 제어 |
| 하드웨어 제어 (Buzzer) | O | SoftTone을 이용한 주파수 제어 및 멜로디 재생 기능 |
| 하드웨어 제어 (CDS) | O | 디지털 입력을 통한 조도 센서 값 모니터링 및 실시간 LED 연동 |
| 하드웨어 제어 (FND) | O | 7447 BCD 디코더 인터페이스를 통한 0~9 숫자 표시 및 카운트다운 |
| Makefile 구현 | O | 타겟별 의존성 관리, 증분 빌드 및 동적 라이브러리 빌드 자동화 |
| TCP/IP 소켓 통신 | O | Protocol 구조체 기반의 명령 송수신 및 클라이언트 접속 대기 로직 |
| 데몬 프로세스 | O | fork, setsid, 입출력 리다이렉션을 통한 백그라운드 서비스 구현 |
| 동적 라이브러리 사용 | O | dlopen, dlsym을 활용한 장치 제어 모듈의 런타임 동적 로딩 |
| 멀티 스레딩 | O | pthread를 이용한 조도 감시, 부저 연주, FND 제어의 독립적 병렬 처리 |
| 시그널 핸들링 | O | 클라이언트 종료 시 하드웨어 자동 OFF 및 SIGINT 시 소켓 자원 정리 |

* 추가기능은 별도로 구현하지 않았습니다. 

